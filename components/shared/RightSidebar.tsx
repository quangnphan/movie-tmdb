import React from "react";

const RightSidebar = () => {
  return (
    <section className="sticky right-0 top-0 z-20 flex h-screen w-fit flex-col justify-between gap-12 overflow-auto border-l px-4 py-4 max-xl:hidden">
      RightSidebar
    </section>
  );
};

export default RightSidebar;
